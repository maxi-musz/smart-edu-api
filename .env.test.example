# Optional: e2e tests use this URL when set (see test/jest-e2e-setup.ts).
# Copy to `.env.test` or add DATABASE_URL_TEST to `.env`.
#
# DATABASE_URL_TEST="postgresql://USER:PASSWORD@HOST:5432/smeh_e2e"
